import 'package:flutter/material.dart';

/// Jetons couleur — maquette mobile v2 « Tour 2 · palette cacao »
/// (design-cacaotrack-gn.md v0.2, arbitrée par le client le 2026-09-21).
/// Thème clair uniquement.
abstract final class AppColors {
  static const cacao900 = Color(0xFF35200F); // encre + bouton plein secondaire
  static const cacao700 = Color(0xFF5A3520);
  static const muted = Color(0xFF6A5A4D);
  static const creme = Color(0xFFFBF6EF);
  static const surface = Color(0xFFFFFFFF);
  static const line = Color(0xFFE3D6C8);
  static const fieldBorder = Color(0xFF8A7768);

  static const foret700 = Color(0xFF1D6A38); // action principale
  static const foretBg = Color(0xFFE2F2E6);

  static const ambre = Color(0xFF7A4B00);
  static const ambreBg = Color(0xFFFFEFC7); // aussi fond de la carte Montant

  static const bleu = Color(0xFF1B5CA6);
  static const bleuBg = Color(0xFFE1ECF8);

  static const rouge = Color(0xFFB3261E);
  static const rougeBg = Color(0xFFFBE3E1);

  static const violet = Color(0xFF6B3FA0);
  static const violetBg = Color(0xFFEEE5F8);

  static const gris = Color(0xFFEFE7DE);
}
